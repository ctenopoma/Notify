apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Fixed UID so the auto-generated alert rules (provisioning/alerting/rules.yml)
    # can reference this datasource deterministically.
    uid: prometheus
    url: http://prometheus:9090
    isDefault: true
